coverage:
  precision: 2
  round: nearest

  status:
    project:
      default:
        target: auto
        threshold: 10
    changes:
      default:
        branches: !*
    patch:
      default:
        branches: !*

comment:
  layout: header
