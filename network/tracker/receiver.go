package tracker

// TODO
// if connection path does not exist, store it
func (t *tracker) storeConnectionPath(cp string) error {
	return nil
}

// TODO
// if right outer peer exists, extend matching connection path
// if left outer peer exists, extend matching connection path
func (t *tracker) extendConnectionPath(cp string) error {
	return nil
}

// TODO
// if peer exists inside, split matching connection path
func (t *tracker) splitConnectionPath(cp string) error {
	return nil
}
